export function toast(msg,ok=true){ let t=document.getElementById('toast'); if(!t){t=document.createElement('div'); t.id='toast'; t.style.position='fixed'; t.style.bottom='16px'; t.style.left='50%'; t.style.transform='translateX(-50%)'; t.style.padding='10px 16px'; t.style.borderRadius='12px'; t.style.background=ok?'rgba(16,185,129,.95)':'rgba(220,38,38,.95)'; t.style.color='#fff'; t.style.zIndex='9999'; document.body.appendChild(t);} t.textContent=msg; t.style.opacity='1'; setTimeout(()=>t.style.opacity='0',2200);} 
export function setupDialogs(){ document.querySelectorAll('[data-dialog-target]').forEach(btn=>{ const id=btn.getAttribute('data-dialog-target'); const dlg=document.getElementById(id); if(!dlg) return; btn.addEventListener('click',()=>dlg.showModal()); dlg.addEventListener('click',(e)=>{ const r=dlg.getBoundingClientRect(); if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom) dlg.close(); }); }); }